# tagprが失敗した場合に手動でタグを打った時のリリース用ジョブ
# 現在はまだ本番環境がないので、動作確認用のサンプルジョブを実行

name: release
on:
  push:
    tags:
    - "v[0-9]+.[0-9]+.[0-9]+"
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
    - uses: ./.github/workflows/sample.yml
      with:
          tag: ${{ github.ref }}
